{
  "name": "any-code",
  "private": true,
  "scripts": {
    "dev:app": "pnpm --filter app dev",
    "build:agent": "pnpm --filter agent build",
    "build:server": "pnpm --filter server build",
    "build": "pnpm -r build",
    "start": "pnpm --filter @any-code/agent build && pnpm --filter app build && pnpm --filter any-code-server build && pnpm --filter any-code-server start"
  },
  "devDependencies": {
    "vitest": "^4.1.0"
  }
}
